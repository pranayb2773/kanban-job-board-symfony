{# This partial renders only the form content for the Job Board creation modal.
   The surrounding modal container and header are defined in _header.html.twig. #}

{{ form_start(job_board_form) }}
    <div class="modal-body">
        {{ form_row(job_board_form.name) }}
        {{ form_row(job_board_form.description) }}
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <button type="submit" class="btn btn-primary">
            <i class="bi bi-check-lg me-1"></i>Create Job Board
        </button>
    </div>
{{ form_end(job_board_form) }}
